[package]
name = "plants_vs_zombies"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.14", features = ["dynamic_linking", "bevy_dev_tools"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
